<?xml version="1.0" encoding="utf-8"?>
<!-- ADR-0030: the Train tab's glyph, hand-authored from Redesign.dc.html's play-triangle icon
     rather than an icon library — no new dependency, the same reasoning StepperField's +/-
     glyphs and DrillDownTopBar's "Back" label already use. Tinted at the call site. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M6,3 L20,12 L6,21 Z"
        android:fillColor="#FF000000" />
</vector>
